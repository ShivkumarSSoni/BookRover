/**
 * Barrel export for all BookRover TypeScript namespace declarations.
 *
 * Import from here in all components and services:
 *   import { BookRover } from '../types';
 */

export { BookRover } from './admin';
